"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.TelemetryClient = exports.FLUSH_TIMEOUT_MS = exports.REQUEST_TIMEOUT_MS = void 0;
const zlib_1 = require("zlib");
const util_1 = require("util");
const timers_1 = __importDefault(require("timers"));
const gzipAsync = (0, util_1.promisify)(zlib_1.gzip);
exports.REQUEST_TIMEOUT_MS = 1_000;
exports.FLUSH_TIMEOUT_MS = 500;
const SCHEMA_VERSION = 'v1';
function noop() {
}
function eventPath(name) {
    return `/${SCHEMA_VERSION}/${name.toLowerCase().replace(/\s+/g, '-')}`;
}
class TelemetryClient {
    constructor(endpoint, fetch = globalThis.fetch.bind(globalThis), flushTimeoutMs = exports.FLUSH_TIMEOUT_MS, requestTimeoutMs = exports.REQUEST_TIMEOUT_MS) {
        this.controller = new AbortController();
        this.pending = new Set();
        this.endpoint = endpoint;
        this.fetch = fetch;
        this.flushTimeoutMs = flushTimeoutMs;
        this.requestTimeoutMs = requestTimeoutMs;
    }
    track(event) {
        const trackPromise = this.doTrack(event)
            .then(noop, noop)
            .finally(() => this.pending.delete(trackPromise));
        this.pending.add(trackPromise);
    }
    async doTrack(event) {
        const payload = event.payload;
        const query = new URLSearchParams({
            sessionId: String(payload.session_id ?? ''),
        });
        const url = `${this.endpoint}${eventPath(event.name)}?${query.toString()}`;
        const compressed = await gzipAsync(Buffer.from(JSON.stringify(event)));
        try {
            const signal = AbortSignal.any([
                AbortSignal.timeout(this.requestTimeoutMs),
                this.controller.signal,
            ]);
            await this.fetch(url, {
                method: 'HEAD',
                headers: { Cookie: `mge=${compressed.toString('base64')}` },
                signal,
            });
        }
        catch {
        }
    }
    async flush() {
        if (this.pending.size !== 0) {
            const maxFlushTimeout = timers_1.default.promises.setTimeout(this.flushTimeoutMs, {
                unref: true,
            });
            await Promise.race([Promise.allSettled(this.pending), maxFlushTimeout]);
        }
        this.controller.abort('TelemetryClient flush');
    }
}
exports.TelemetryClient = TelemetryClient;
//# sourceMappingURL=telemetry-client.js.map