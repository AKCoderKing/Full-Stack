import type { TelemetryEvent } from './telemetry-events';
import type { MongoshAnalytics } from './analytics-helpers';
export declare const REQUEST_TIMEOUT_MS = 1000;
export declare const FLUSH_TIMEOUT_MS = 500;
type FetchFn = (url: string, init?: {
    method?: string;
    headers?: {
        Cookie: string;
    };
    signal?: AbortSignal;
}) => Promise<unknown>;
export declare class TelemetryClient implements MongoshAnalytics {
    private readonly endpoint;
    private readonly fetch;
    private readonly requestTimeoutMs;
    private readonly flushTimeoutMs;
    private readonly controller;
    private pending;
    constructor(endpoint: string, fetch?: FetchFn, flushTimeoutMs?: number, requestTimeoutMs?: number);
    track(event: TelemetryEvent): void;
    private doTrack;
    flush(): Promise<void>;
}
export {};
