"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.ThrottledAnalytics = exports.ToggleableAnalytics = exports.NoopAnalytics = void 0;
const fs_1 = __importDefault(require("fs"));
const path_1 = __importDefault(require("path"));
class Queue {
    constructor(applyFn) {
        this.queue = [];
        this.state = 'paused';
        this.applyFn = applyFn;
    }
    push(val) {
        switch (this.state) {
            case 'paused':
                this.queue.push(val);
                return;
            case 'enabled':
                this.applyFn(val);
                return;
            case 'disabled':
            default:
                return;
        }
    }
    enable() {
        this.state = 'enabled';
        const queue = this.queue;
        this.queue = [];
        queue.forEach((val) => {
            this.applyFn(val);
        });
    }
    disable() {
        this.state = 'disabled';
        this.queue = [];
    }
    pause() {
        this.state = 'paused';
    }
    getState() {
        return this.state;
    }
}
class NoopAnalytics {
    track() {
    }
    flush() {
        return Promise.resolve();
    }
}
exports.NoopAnalytics = NoopAnalytics;
class ToggleableAnalytics {
    constructor(target = new NoopAnalytics()) {
        this._queue = new Queue((event) => {
            this._target.track(event);
        });
        this._target = target;
    }
    track(event) {
        this._queue.push(event);
    }
    enable() {
        this._queue.enable();
    }
    disable() {
        this._queue.disable();
    }
    pause() {
        this._queue.pause();
    }
    async flush() {
        return await this._target.flush();
    }
}
exports.ToggleableAnalytics = ToggleableAnalytics;
async function lockfile(filepath, staleDuration = 43_200_000) {
    let intervalId;
    const lockfilePath = `${filepath}.lock`;
    const unlock = async () => {
        clearInterval(intervalId);
        try {
            return await fs_1.default.promises.rmdir(lockfilePath);
        }
        catch {
        }
    };
    try {
        await fs_1.default.promises.mkdir(lockfilePath);
        intervalId = setInterval(() => {
            const now = Date.now();
            fs_1.default.promises.utimes(lockfilePath, now, now).catch(() => {
            });
        }, staleDuration / 2);
        intervalId.unref?.();
        return unlock;
    }
    catch (e) {
        if (e.code !== 'EEXIST') {
            throw e;
        }
        const stats = await fs_1.default.promises.stat(lockfilePath);
        if (Date.now() - stats.mtimeMs > staleDuration) {
            await fs_1.default.promises.rmdir(lockfilePath);
            return lockfile(filepath, staleDuration);
        }
        throw new Error(`File ${filepath} already locked`);
    }
}
class ThrottledAnalytics {
    constructor({ target, currentSessionId, throttle, } = {}) {
        this.trackQueue = new Queue((event) => {
            if (this.shouldEmitAnalyticsEvent()) {
                this.target.track(event);
                this.throttleState.count++;
            }
        });
        this.currentSessionId = null;
        this.throttleOptions = null;
        this.throttleState = { count: 0, timestamp: Date.now() };
        this.restorePromise = Promise.resolve();
        this.unlock = () => Promise.resolve();
        this.target = target ?? new NoopAnalytics();
        this.throttleOptions = throttle ?? this.throttleOptions;
        if (currentSessionId) {
            this.beginRestore(currentSessionId);
        }
    }
    beginRestore(sessionId) {
        this.currentSessionId = sessionId;
        this.restorePromise = this.restoreThrottleState().then((enabled) => {
            if (!enabled) {
                this.trackQueue.disable();
                return;
            }
            this.trackQueue.enable();
        });
    }
    get metadataPath() {
        if (!this.throttleOptions) {
            throw new Error('Metadata path is not available if throttling is disabled');
        }
        if (!this.currentSessionId) {
            throw new Error('Metadata path is not available if sessionId is not set');
        }
        return path_1.default.resolve(this.throttleOptions.metadataPath, `am-${this.currentSessionId}.json`);
    }
    track(event) {
        if (!this.currentSessionId) {
            this.beginRestore(event.payload.session_id);
        }
        this.trackQueue.push(event);
    }
    async restoreThrottleState() {
        if (!this.throttleOptions) {
            return true;
        }
        if (!this.currentSessionId) {
            throw new Error('Trying to restore throttle state before sessionId is set');
        }
        try {
            this.unlock = await lockfile(this.metadataPath, this.throttleOptions.lockfileStaleDuration);
        }
        catch (e) {
            return false;
        }
        try {
            this.throttleState = JSON.parse(await fs_1.default.promises.readFile(this.metadataPath, 'utf8'));
        }
        catch (e) {
            if (e.code !== 'ENOENT') {
                return false;
            }
        }
        return true;
    }
    shouldEmitAnalyticsEvent() {
        if (!this.throttleOptions) {
            return true;
        }
        if (Date.now() - this.throttleState.timestamp >
            (this.throttleOptions.timeframe ?? 60_000)) {
            this.throttleState.timestamp = Date.now();
            this.throttleState.count = 0;
            return true;
        }
        return this.throttleState.count < this.throttleOptions.rate;
    }
    async flush() {
        if (!this.throttleOptions) {
            await this.target.flush();
            return;
        }
        if (!this.currentSessionId) {
            throw new Error('Trying to persist throttle state before sessionId is set');
        }
        try {
            await this.restorePromise;
        }
        catch {
        }
        await fs_1.default.promises.writeFile(this.metadataPath, JSON.stringify(this.throttleState));
        await this.unlock();
        await this.target.flush();
    }
}
exports.ThrottledAnalytics = ThrottledAnalytics;
//# sourceMappingURL=analytics-helpers.js.map